#ifndef CALCULATOR_H
#define CALCULATOR_H

class Calculator
{
    public:
        int Add (int, int);
        int Mul (double, double);
        int Sub (double, double);
        double Div (double, double);
        int Pow (double, double);
};

#endif//CALCULATOR_H
